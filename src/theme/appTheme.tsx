import {StyleSheet} from 'react-native';

export const globalStyles = () =>
  StyleSheet.create({
    title: {
      fontSize: 35,
      fontWeight: 'bold',
    },
    globalMargin: {
      marginHorizontal: 20,
    },
  });
